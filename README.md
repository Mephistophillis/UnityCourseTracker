# Unity Course Progress Tracker

Минималистичный сервис для отслеживания прогресса группы студентов по курсу Unity.
Реализован на React + Vite, использует Telegram для авторизации и localStorage для хранения данных.

## 📋 Требования

Для работы проекта требуется:
1.  **Node.js** (версия 18+)
2.  **Telegram Bot** (для авторизации)

## 🚀 Быстрый старт

1.  **Установка зависимостей**
    ```bash
    npm install
    ```

2.  **Настройка окружения**
    Создайте файл `.env` в корне проекта (можно скопировать `.env.example`):
    ```env
    VITE_TELEGRAM_BOT_NAME=your_bot_username
    ```

3.  **Запуск локально**
    ```bash
    npm run dev
    ```

## 🤖 Настройка Telegram Бота (Пошаговый план)

Для того чтобы кнопка "Войти через Telegram" заработала, необходимо создать и настроить бота.

### Шаг 1: Создание бота
1.  Откройте Telegram и найдите бота **@BotFather**.
2.  Отправьте команду `/newbot`.
3.  Введите имя бота (например, `UnityCourseTracker`).
4.  Введите юзернейм бота (должен заканчиваться на `bot`, например `UnityTracker_bot`).
5.  **Важно:** Скопируйте полученный **Token** (хотя для фронтенда нам нужен только username, токен пригодится если вы будете делать бэкенд).
6.  Вставьте юзернейм бота (без @) в файл `.env`:
    ```
    VITE_TELEGRAM_BOT_NAME=ВашЮзернеймБота
    ```

### Шаг 2: Настройка домена
Telegram Widget требует привязки домена, на котором он будет работать.

1.  В **@BotFather** отправьте команду `/setdomain`.
2.  Выберите вашего бота.
3.  Введите домен вашего сайта.
    *   **Для GitHub Pages:** `https://<ваш-логин>.github.io`
    *   **Для локальной разработки:** Telegram не поддерживает `localhost` напрямую. Вам нужно либо использовать "туннель" (например, ngrok), либо прописать фиктивный домен в `/etc/hosts` и настроить HTTPS.
    *   *Простой вариант для теста:* Просто задеплойте на GitHub Pages и укажите этот домен.

## 📦 Деплой на GitHub Pages

Этот проект использует GitHub Actions для автоматического деплоя.

1.  **Подготовка Репозитория**
    *   Создайте репозиторий на GitHub.
    *   В настройках репозитория перейдите в **Settings -> Secrets and variables -> Actions**.
    *   Во вкладке **Variables** (или Secrets, но Variables здесь достаточно, так как это не секрет) нажмите **New repository variable**.
    *   Name: `VITE_TELEGRAM_BOT_NAME`
    *   Value: `ВашЮзернеймБота` (без @)

2.  **Запуск**
    *   Запушьте код в ветку `main`.
    *   Перейдите во вкладку **Actions** в репозитории — вы увидите запущенный воркфлоу `Deploy to GitHub Pages`.
    *   После успешного завершения, сайт будет доступен по адресу, указанному в логах деплоя (обычно `https://<user>.github.io/cource-runners/`).

**Важно:** Файл `.env` добавлен в `.gitignore` и не попадет в репозиторий. Для локальной разработки используйте свой локальный `.env`.

## 🛠 Структура проекта

- `public/users.json` — Начальный список пользователей.
- `public/course.json` — Структура курса.
- `src/components/` — Компоненты (Login, Home, Participant).
- `src/utils/storage.js` — Логика работы с localStorage.
